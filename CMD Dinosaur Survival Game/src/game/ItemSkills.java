package game;

/**
 * The enum class that covers the skills needed for items. Such as being able to feed carnivores or herbivores and being
 * able to tag dinosaurs.
 */
public enum ItemSkills {
    CAN_FEED_HERBIVORE, CAN_FEED_CARNIVORE, CAN_FEED_MARINE, CAN_TAG;
}
